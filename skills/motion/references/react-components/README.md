# React Components

| Name | Description | Path |
|------|-------------|------|
| AnimatePresence | Enables exit animations for `motion` components when they are removed from the React tree, unlocking the `exit` prop on direct children. | [animate-presence.md](./animate-presence.md) |
| AnimateView | Motion+ Early Access component (3kb) that animates transitions between views using the browser's native View Transition… | [animate-view.md](./animate-view.md) |
| LayoutGroup | Groups `motion` components that might not render together but affect each other's layout, so sibling layout animations… | [layout-group.md](./layout-group.md) |
| LazyMotion | Reduces bundle size by loading Motion's animation features on demand, cutting the initial bundle from ~34kb to ~4.6kb… | [lazy-motion.md](./lazy-motion.md) |
| MotionConfig | Wrapper that sets configuration defaults (transition, reduced-motion behavior, CSP nonce) for all descendant `motion`… | [motion-config.md](./motion-config.md) |
| motion | Drop-in replacement for HTML and SVG elements that adds animation props running at up to 120fps without triggering… | [motion.md](./motion.md) |
| Reorder | Drag-to-reorder lists with automatic layout animations. `Reorder.Group` wraps the list and manages ordering;… | [reorder.md](./reorder.md) |

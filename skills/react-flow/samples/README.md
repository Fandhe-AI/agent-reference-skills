# samples

| Name | Description | Path |
|------|-------------|------|
| Auto Layout with Dagre | Dagre を使ってノードを自動配置する。方向（TB / LR）を切り替えるボタンも実装する。 | [auto-layout-dagre.md](./auto-layout-dagre.md) |
| Basic Flow | 静的なノードとエッジを定義し、ReactFlow コンポーネントで描画する最小構成。 | [basic-flow.md](./basic-flow.md) |
| Computing Flows | ノードをデータ変換の処理単位として扱い、接続されたノードのデータを読み取って計算結果を書き込む。 | [computing-flows.md](./computing-flows.md) |
| Custom Edges | `BaseEdge` とパスユーティリティを使って独自のエッジコンポーネントを定義し `edgeTypes` に登録する。 | [custom-edges.md](./custom-edges.md) |
| Custom Nodes | 独自の React コンポーネントをノードとして使用する。`nodeTypes` に登録し `ReactFlow` へ渡す。 | [custom-nodes.md](./custom-nodes.md) |
| Interactive Flow | `onNodesChange` / `onEdgesChange` / `onConnect` を実装し、ドラッグ・接続・削除をインタラクティブに動作させる。 | [interactive-flow.md](./interactive-flow.md) |
| State Management with Zustand | Zustand ストアでノード・エッジ状態を集中管理し、カスタムノードからストアのアクションを直接呼び出す。 | [state-management-zustand.md](./state-management-zustand.md) |
| Sub Flows | `parentId` を使って子ノードを親ノード内に配置し、グループ化・階層構造を実現する。 | [sub-flows.md](./sub-flows.md) |
| useReactFlow Hook | `useReactFlow` でノード・エッジの操作とビューポート制御を行う。 | [use-react-flow-hook.md](./use-react-flow-hook.md) |

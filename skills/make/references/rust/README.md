# rust

| Name | Description | Path |
| --- | --- | --- |
| Crate and Workspace Profiles | Single crate vs root-package workspace vs virtual workspace; fmt/clippy/build/test/doc scope differences; `--all-targets` vs doctests; feature flag strategy; Cargo.lock policy; `--locked`/`--offline`/`--frozen` | [crate-and-workspace-profiles.md](./crate-and-workspace-profiles.md) |
| xtask and Validation | The (unofficial) cargo-xtask pattern: `.cargo/config.toml` alias, workspace membership, subprocess/exit-code propagation, when to adopt it vs. a shell script vs. keeping just/Task | [xtask-and-validation.md](./xtask-and-validation.md) |
